const directionChanged = (direction) => {
    //....
}

export const changeDirection = (e) => {
    let direction = '';
    switch(e.code) {
        case 'KeyW':
            //.
            break;
        case 'KeyA':
            //.
            break;
        case 'KeyS':
            //.
            break;
        case 'KeyD':
            //.
            break;
        default:
            break;
    }

    directionChanged(direction);
}